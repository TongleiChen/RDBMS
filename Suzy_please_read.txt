😭
直到0416: 
✅1. UPDATE 可以只pass string 给我 不转换成int没关系的 就和你的insert差不多？
✅2. 整理一下select的语法树 等待ctl跟我会议 
✅3. 加GROUP BY-HAVING condition 
✅4. HAVING SUM() COUNT() AVG() MAX() MIN() (我们group by 简化为只跟一个column吧！！！)
✅5. select语法树中添加join的语法: 
Minimum of a 2-table theta-join:1. 对两张表进行笛卡尔积；2. 在得到的结果中选择合适的项。
********************************************
SELECT column1, column2, ...
FROM table1
[INNER | LEFT | RIGHT | FULL] JOIN table2
ON condition1 <operator> condition2;
********************************************
✅6. query的语法不严谨，name = 'Suzy'要死了 好多要改 要死了 （select✅; create不用; drop不用; update✅; delete✅）
7. 提问：foreign key是有分cascade和no cascade两种check情况，还是不加cascade也要check并且如果有冲突就报错？
8. create index语法

select grammar supports:
SELECT selection from_clause options*
selection: * | column1, column2, column3 | SUM(column) | MAX(column) | MIN(column) 
from_clause: FROM table
options:    where_clause: WHERE condition1 (AND/OR condition2)| age BETWEEN 2 AND 4 
    | order_by_clause: ORDER BY age ASC/DESC
    | group_having_clause
    | theta_join_clause
    | group_having_clause: 

TODO:
1. 加GROUP BY-HAVING condition
2. HAVING SUM() COUNT() AVG() MAX() MIN()


0415:
1. 你传给我的CREATE_tree_Evaluator里面的table_name是Token('CNAME', 'customers')这样的。能不能只要一个table的string哇
2. 0414的提问2你是不是回答过我了but我忘记了
3. 我真的热死了 我真的热死了 我真的热死了
4. 是只有delete有cascade吗啊啊啊啊我还是不会
5. Create index的grammar


